let runGame = () => {
  // TODO game logic
}

// use with document.addEventListener("keydown", handleInput);
let handleInput = (event) => {
  const pressedKey = event.code; //event.which & event.keyCode are deprecated

  // TODO input -> movement
}